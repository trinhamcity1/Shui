# Shui.xcodeproj is committed (generated by XcodeGen from project.yml —
# see README) so the app opens directly with no XcodeGen install required.
# Re-run `xcodegen generate` and commit the result after editing project.yml.

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but ignore if introduced later)
Pods/

# Placeholder video generator output — regenerate with
# scripts/placeholder_videos/generate_placeholders.py; the copies actually
# bundled into the app live in Shui/Resources/Videos/.
scripts/placeholder_videos/out/
